@model FleetManagement.Application.Aircrafts.DTOs.AircraftDto

<div class="modal-content">
    <div class="modal-header">
        <h2>@(Model.Id == 0 ? "Add New Aircraft" : "Edit Aircraft")</h2>
        <button type="button" class="close-btn" data-action="close-modal" aria-label="Close">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>
        </button>
    </div>
    
    <form id="aircraftForm" asp-action="@(Model.Id == 0 ? "Create" : "Edit")" asp-route-id="@Model.Id" method="post" novalidate>
        @Html.AntiForgeryToken()
        @Html.HiddenFor(m => m.Id)
        
        <div class="modal-body">
            <!-- Basic Information -->
            <div class="form-section">
                <div class="form-row">
                    <div class="form-group">
                        <label asp-for="RegistrationNumber" class="form-label">Registration Number</label>
                        <input asp-for="RegistrationNumber" class="form-input" placeholder="e.g., N12345" autocomplete="off" />
                        <span asp-validation-for="RegistrationNumber" class="validation-error"></span>
                    </div>
                    
                    <div class="form-group">
                        <label asp-for="SerialNumber" class="form-label">Serial Number</label>
                        <input asp-for="SerialNumber" class="form-input" placeholder="e.g., SN-2023-001" autocomplete="off" />
                        <span asp-validation-for="SerialNumber" class="validation-error"></span>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label asp-for="Model" class="form-label">Model</label>
                        <input asp-for="Model" class="form-input" placeholder="e.g., Boeing 737-800" autocomplete="off" />
                        <span asp-validation-for="Model" class="validation-error"></span>
                    </div>
                    
                    <div class="form-group">
                        <label asp-for="Manufacturer" class="form-label">Manufacturer</label>
                        <select asp-for="Manufacturer" class="form-input" asp-items="@(new SelectList(ViewBag.Manufacturers, "Value", "Text"))">
                            <option value="">-- Select Manufacturer --</option>
                        </select>
                        <span asp-validation-for="Manufacturer" class="validation-error"></span>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label asp-for="YearOfManufacture" class="form-label">Year of Manufacture</label>
                        <input asp-for="YearOfManufacture" type="number" class="form-input" placeholder="e.g., 2023" min="1900" max="2100" />
                        <span asp-validation-for="YearOfManufacture" class="validation-error"></span>
                    </div>
                    
                    <div class="form-group">
                        <label asp-for="Status" class="form-label">Status</label>
                        <select asp-for="Status" class="form-input" asp-items="@(new SelectList(ViewBag.Statuses, "Value", "Text"))">
                            <option value="">-- Select Status --</option>
                        </select>
                        <span asp-validation-for="Status" class="validation-error"></span>
                    </div>
                </div>
            </div>

            <!-- Specifications -->
            <div class="form-section">
                <div class="form-row">
                    <div class="form-group">
                        <label asp-for="Specification.BasedStation" class="form-label">Based Station</label>
                        <input asp-for="Specification.BasedStation" class="form-input" placeholder="e.g., JFK International" autocomplete="off" />
                        <span asp-validation-for="Specification.BasedStation" class="validation-error"></span>
                    </div>
                    
                    <div class="form-group">
                        <label asp-for="Specification.SeatingCapacity" class="form-label">Seating Capacity</label>
                        <input asp-for="Specification.SeatingCapacity" type="number" class="form-input" placeholder="e.g., 189" min="0" />
                        <span asp-validation-for="Specification.SeatingCapacity" class="validation-error"></span>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label asp-for="Specification.MaxTakeoffWeight" class="form-label">Max Takeoff Weight</label>
                        <input asp-for="Specification.MaxTakeoffWeight" type="number" step="0.01" class="form-input" placeholder="e.g., 79015.8" />
                        <span asp-validation-for="Specification.MaxTakeoffWeight" class="validation-error"></span>
                    </div>
                    
                    <div class="form-group">
                        <label asp-for="Specification.MaxLandingWeight" class="form-label">Max Landing Weight</label>
                        <input asp-for="Specification.MaxLandingWeight" type="number" step="0.01" class="form-input" placeholder="e.g., 66361" />
                        <span asp-validation-for="Specification.MaxLandingWeight" class="validation-error"></span>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label asp-for="Specification.WeightUnit" class="form-label">Weight Unit</label>
                        <select asp-for="Specification.WeightUnit" class="form-input" asp-items="@(new SelectList(ViewBag.WeightUnits, "Value", "Text"))">
                            <option value="">-- Select Unit --</option>
                        </select>
                        <span asp-validation-for="Specification.WeightUnit" class="validation-error"></span>
                    </div>
                </div>
            </div>
        </div>

        <div class="modal-footer">
            <button type="button"
                    class="btn btn-secondary"
                    data-action="close-modal">
                Cancel
            </button>

            <button type="submit"
                    class="btn btn-primary"
                    id="submitBtn">
                @(Model.Id == 0 ? "Create Aircraft" : "Save Changes")
            </button>
        </div>

    </form>
</div>
